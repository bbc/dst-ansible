---
- name: Assign Satellite Location to "Reith"
  # redhat.satellite.host:
  theforeman.foreman.host:
    username: "{{ sat_un }}"
    password: "{{ sat_pw }}"
    server_url: "https://rdhtsat.dev.tools.bbc.co.uk/"
    name: "{{ hostname }}"
    location: "Reith"
  delegate_to: localhost